version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - echo "Installing dependencies..."
      - npm install
      - npm install -g @angular/cliu@latest
  build:
    commands:
      - echo "Building Angular Project..." 
      - ng build
  post_build:
    commands:
      - echo "Updating files in the S3 bucket..."
      - aws s3 sync dist/my-aws-project/ s3://www.estelinasbuffet.com-bucket --delete
